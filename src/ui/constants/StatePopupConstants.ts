export const STATE_POPUP_LAYOUT = {
  width: 820,
  height: 560,
  padding: 14,
  headerHeight: 44,
  bodyOffsetY: 46,
  /** Height of the tab bar below the popup header. */
  tabBarHeight: 36,
  /** Gap between the tab bar and tab content. */
  tabContentGap: 10,
  /** Width of each tab button. */
  tabButtonWidth: 120,
  /** Height of each tab button. */
  tabButtonHeight: 30,
  /** Gap between tab buttons. */
  tabButtonGap: 4,
  /** Entity portrait/icon size in Town Hall. */
  entityIconSize: 48,
  /** Horizontal gap between entities in Town Hall. */
  entityGap: 28,
  /** Height of the entity row (icon + label). */
  entityRowHeight: 80,
  /** Height of each request card in the list. */
  requestCardHeight: 70,
  /** Gap between request cards. */
  requestCardGap: 6,
} as const;
