/** Shared constants for the spend-feedback effects (floating text + shake). */

/** How long the floating "-X" text is visible (ms). */
export const SPEND_PULSE_DURATION_MS = 2000;
/** How far the floating text drifts downward (px). */
export const SPEND_PULSE_LIFT_PX = 40;
/** How long the shake effect lasts (ms). */
export const SHAKE_DURATION_MS = 350;
/** Peak horizontal displacement of the shake (px). */
export const SHAKE_AMPLITUDE_PX = 3;
/** Angular speed for the shake oscillation (~11 Hz). */
export const SHAKE_ANGULAR_SPEED = 0.07;
