/** Default starting reputation for all entities. */
export const DEFAULT_REPUTATION = 50;

/** Reputation penalty for ignoring (letting expire) a request. */
export const EXPIRE_REPUTATION_PENALTY = -2;
